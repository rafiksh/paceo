/**
  Use these spacings for margins/paddings and other whitespace throughout your app.
 */
export const spacing = {
  xxxs: 2,
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
} as const

/**
  Use these radius values for consistent border radius throughout your app.
 */
export const radius = {
  xs: 4, // Small elements (checkboxes, small buttons)
  sm: 8, // Medium elements (input fields, small cards)
  md: 12, // Standard elements (buttons, cards, inputs)
  lg: 16, // Large elements (large cards, containers)
  xl: 20, // Extra large elements (modals, large containers)
} as const
